1. What is an API? 

application programming interface - its a messenger between 2 services


2. Types of APIs (REST, SOAP, Public, Private) 

REST - most common - endpoints - uses url 
SOAP - old - simple object access protocol
public - open to everyone - maps api - twitter api - stripe payment 
private - internal use only - 


3. HTTP Methods (GET, POST, PUT, DELETE) 





# GET → Read data
@app.get("/users")
def get_all_users():
    return users


@app.get("/users/{user_id}")
def get_user(user_id: int):
    return users[user_id]

# POST → Create data
@app.post("/users/{user_id}")
def create_user(user_id: int, user: User):
    users[user_id] = user
    return {"message": "User created", "data": user}


# PUT → Update data

@app.put("/users/{user_id}")
def update_user(user_id: int, user: User):
    users[user_id] = user
    return {"message": "User updated", "data": user}

# DELETE → Remove data
@app.delete("/users/{user_id}")
def delete_user(user_id: int):
    del users[user_id]
    return {"message": "User deleted"}


4.  API Endpoints and URL structure 

Query Parameters
GET /users?age=25
GET /orders?status=shipped&limit=10


  Request and Response format (JSON) 
  HTTP Status Codes (200, 404, 500, etc.) 

200 - ok
201 - created 
204 - no content
400 - bad requests
401 - unauth
403 - forbidden 
404 - not found
500 - internal server error 
502 -bad gateway 
503 -service unavailable

  Authentication methods (API Key, OAuth 2.0, Bearer Token) 

api key -client sends a secret key with every request.
bearer token(jwt)- user logs in -> server gives a token -> send token in header
oath  - thrid party login - using google apple,




  Making API calls using code (Python requests, JavaScript fetch) 
import requests
respnse = requests.get(endpoints, headers = headers)


fetch(endpoint).then(res => res.json())

get using js 
fetch(endpoint)
  .then(res => res.json())
  .then(data => console.log(data));

post 
fetch(endpoint, {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    name: "Suman",
    email: "suman@mail.com"
  })
})
.then(res => res.json())
.then(data => console.log(data));


put 
fetch(endpoint, {
  method: "PUT",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    name: "Suman Updated",
    email: "suman@mail.com"
  })
});


  API rate limiting and quotas 
  Pagination in API responses 
  Error handling and debugging API responses 
  Reading and using API documentation effectively 
  Working with environment variables and secrets 
  Real-world API examples (Twitter, Salesforce, Facebook) 